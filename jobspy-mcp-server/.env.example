# Docker image name for the JobSpy scraper container
JOBSPY_DOCKER_IMAGE=jobspy

# SSE server bind address (used when ENABLE_SSE=1)
JOBSPY_HOST=localhost
JOBSPY_PORT=9423

# Set to 1 to expose the MCP server over HTTP/SSE (required for the Streamlit app default)
ENABLE_SSE=1
